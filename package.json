{
  "name": "ndc-sizer",
  "version": "1.0.0",
  "description": "Netwrix Data Sizer - NDC hardware sizing tool",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev:server": "node server.js",
    "dev:client": "cd client && npm run dev",
    "build:ui": "cd client && npm install && npm run build && cd .. && node build-inline.js",
    "build": "npm run build:ui && pkg server.js --targets node18-win-x64 --output dist/NDC-Sizer.exe --compress GZip"
  },
  "pkg": {
    "scripts": ["inline-ui.js"],
    "targets": ["node18-win-x64"],
    "outputPath": "dist"
  },
  "dependencies": {
    "adm-zip": "^0.5.17",
    "exceljs": "^4.3.0",
    "express": "^4.18.2",
    "pdf-lib": "^1.17.1"
  }
}
